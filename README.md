# Agent Loop

一个用于教学和方案穿刺的最小 Agent Loop 实现。

当前设计见 [docs/DESIGN.md](docs/DESIGN.md)。实现遵循几个核心约束：

- Agent 只能提出动作，不能自行宣布成功；
- 所有副作用都经过受控工具；
- 只有独立 Verifier 的证据可以产生 `completed`；
- 每一轮都有预算、状态持久化和结构化事件；
- 默认在隔离工作区中运行。

项目仍在开发中。完成首个可运行版本后，本文件会补充安装和教程。
